---
import CollectionHeader from "@/components/common/CollectionHeader.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";

interface Props {
    heading: string;
    tags: string[];
}

const { heading, tags } = Astro.props;
---

<BaseLayout>
    <CollectionHeader heading={heading} tags={tags} />
</BaseLayout>
